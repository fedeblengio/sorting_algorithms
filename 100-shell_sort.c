#include "sort.h"

/**
 *intercambiador_shell - blblblbl
 *@array: random number
 *@l: integer int
 *@size: array size
 *@intervalo: interval
 *Return: cero
 */
void shell_sort(int *array, size_t size){

	if (array || size)
	{
		return;
	}
}
